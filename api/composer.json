{
    "name": "g7/api",
    "autoload": {
        "psr-4": {
            "": [
                "src/",
                "src/tipos"
            ]
        }
    },
    "scripts": {
        "start": "php -S localhost:3000",
        "test": "kahlan",
        "dump": "composer dump-autoload"
    },
    "require": {
        "phputil/router": "^0.3.6",
        "phputil/cors": "^0.5.3"
    },
    "require-dev": {
        "kahlan/kahlan": "^6.1"
    }
}
